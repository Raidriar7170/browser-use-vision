# Benchmarks

Development-time benchmarks for the vision pipeline.

## Scripts

- `run.py` — DOM confidence scoring benchmark (evaluates adaptive strategy decisions)

## Results

- `adaptive_strategy_*.json` — Adaptive strategy decisions across test pages
- `vision_api_perf_*.json` — Vision API (Florence-2) latency measurements

> **Note:** These are development snapshots, not formal evaluation results.
> For real-world effectiveness, see the E2E test results in `output/e2e_results/`.
